distance: cosine
max_fit_samples: 10000
consensus_threshold: 0.5
min_consensus_size: 1              # only applies to the ensemble consensus; inert for a single algorithm
algorithms:
  kmeans:
    n_clusters: [2, 4, 8, 16, 32]

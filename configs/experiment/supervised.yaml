# @package _global_
defaults:
  - override /model: numerical_classifier
  - override /loss: joint_sup_con_ce # cross_entropy
  - override /loops: batch_sampler # default
  - override /optimizer: adamw
  - override /scheduler: one_cycle
  - override /sampler: cluster_batch # null

name: supervised
ignore_classes: null # [0]
n_samples: null

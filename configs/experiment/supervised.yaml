# @package _global_
defaults:
  - override /model: numerical_contrastive_classifier # numerical_classifier
  - override /loss: joint_sup_con_ce
  - override /loops: batch_sampler
  - override /optimizer: adamw
  - override /scheduler: one_cycle
  - override /sampler: rival_batch

name: supervised
ignore_classes: null # [0]
n_samples: null

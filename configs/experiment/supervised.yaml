# @package _global_
defaults:
  - override /model: tabular_classifier
  - override /loss: classification
  - override /loops: default
  - override /optimizer: adamw
  - override /scheduler: one_cycle

model:
  params:
    num_classes: 11

name: supervised
ignore_classes: [0]  # List of class indices to ignore during evaluation
n_samples: null
